
import 'package:flutter/material.dart';

class ShopProfileScreen extends StatelessWidget {
  const ShopProfileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('ShopProfileScreen')
      ],
    );
  }
}
